export type NavItem = {
  label: string;
  href: string;
};

export type ProofItem = {
  value: string;
  label: string;
};

export type FeatureCard = {
  title: string;
  body: string;
  eyebrow: string;
};

export type FlowStep = {
  title: string;
  body: string;
};

